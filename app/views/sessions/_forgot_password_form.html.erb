<%= form_tag password_resets_path, :method => :post, :class => 'well' do %>
	<h1>Forgot Password?</h1>

  	<%= label_tag :email %>
  	<%= text_field_tag :email %> <%= submit_tag "Reset my password!", :class => 'btn btn-success' %>

<% end %>